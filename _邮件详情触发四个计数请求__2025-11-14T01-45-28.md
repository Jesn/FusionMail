[ ] NAME:Current Task List DESCRIPTION:Root task for conversation __NEW_AGENT__
-[x] NAME:SSE 全局订阅改造 - 调研/方案确认 DESCRIPTION:梳理现状与目标；确认 Cookie+HttpOnly 为主、polyfill 为辅的方案；输出落地文档 sse-event.md（已完成）。
--[x] NAME:后端：新增 /api/v1/events SSE 端点（Cookie 鉴权 + CORS + 心跳） DESCRIPTION:实现基于 Cookie(HttpOnly) 的 SSE：校验 fm_session；返回 text/event-stream；启用 withCredentials；配置 CORS 允许 http://localhost:4444 且 Allow-Credentials；实现 20-30s 心跳与 Last-Event-ID 支持；断开清理资源。
--[x] NAME:后端：事件 Hub 与业务广播集成 DESCRIPTION:实现连接管理与广播 Hub；在标记已读/未读、星标/取消、归档/恢复、删除/恢复、新邮件入库等服务层成功后统一触发 email_counts_maybe_changed；合并广播避免风暴；处理慢消费者。
--[x] NAME:前端：在 MainLayout 建立一次 SSE 订阅（Cookie，去抖 400ms） DESCRIPTION:MainLayout 顶层用 EventSource(url, { withCredentials: true }) 订阅 topic=email_counts；收到事件去抖后调用 getGlobalStats()；首屏仍做一次初始拉取；卸载关闭连接；可选 BroadcastChannel 跨 Tab 去重。
--[x] NAME:前端：移除详情页强制刷新统计并做乐观更新 DESCRIPTION:去掉 markAsRead 后的 loadGlobalStats(true) 等强刷；本地 unreadCount-- 乐观更新，最终由 SSE 事件触发的拉取对齐；确保 useEmails 去重不重复拉取。
--[ ] NAME:接口优化：新增 GET /api/v1/emails/stats 聚合接口 DESCRIPTION:返回 { unread, starred, archived, deleted }；前端将四次列表 total 拉取收敛为一次；保留旧逻辑作为回退；灰度切换。
--[/] NAME:基础设施：本地/生产 CORS 与反向代理配置 DESCRIPTION:开发与生产环境分别配置 CORS（Allow-Origin 精确、Allow-Credentials）与 Nginx（HTTP/1.1、关闭 proxy_buffering、延长 read_timeout）；验证心跳与断线重连路径。
--[ ] NAME:可选回退：polyfill 路径与开关 DESCRIPTION:集成 event-source-polyfill 仅在 Cookie 不可用场景启用（传 Authorization 头）；通过特性开关/环境变量控制；验证与 Cookie 路径互斥。
--[ ] NAME:验收与监控 DESCRIPTION:按 sse-event.md 验收清单手测；补充服务端连接/心跳/广播日志与指标；观察事件风暴与慢消费者；制定回滚方案。
-[ ] NAME:Current Task List DESCRIPTION:Root task for conversation __NEW_AGENT__