// Tailwind CSS v4 uses a CSS-first configuration.
// This placeholder exists only for tooling compatibility (e.g., shadcn/ui CLI).
// Keep theme tokens and plugins in CSS: see `src/index.css` (@theme, @plugin).
export default {};